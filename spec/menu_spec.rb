require 'menu'

describe Menu do
  describe Menu do
  subject(:menu) {described_class.new(item, price)}
  let(:item) {{:chicken => 10, :fish => 12, :veg => 8 }}
  end

    it 'prints the menu' do
      @menu
    end
end
